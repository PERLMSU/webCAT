<nav class="breadcrumb" aria-label="breadcrumbs">
    <ul>
        <li><%= link("Classrooms", to: Routes.classroom_path(@conn, :index)) %></li>
        <li><%= link(@changeset.data.section.semester.classroom.name, to: Routes.classroom_path(@conn, :show, @changeset.data.section.semester.classroom_id)) %></li>
        <li><%= link("Semesters", to: Routes.semester_path(@conn, :index, @changeset.data.section.semester.classroom_id)) %></li>
        <li><%= link(@changeset.data.section.semester.name, to: Routes.semester_path(@conn, :show, @changeset.data.section.semester.classroom_id, @changeset.data.section.semester_id)) %></li>
        <li><%= link("Sections", to: Routes.section_path(@conn, :index, @changeset.data.section.semester_id))%></li>
        <li><%= link(@changeset.data.section.number, to: Routes.section_path(@conn, :show, @changeset.data.section.semester_id, @changeset.data.section_id)) %></li>
        <li><%= link("Rotations", to: Routes.rotation_path(@conn, :index, @changeset.data.section_id)) %></li>
        <li><%= link(@changeset.data.number, to: Routes.rotation_path(@conn, :show, @changeset.data.section_id, @changeset.data.id)) %></li>
        <li class="is-active"><%= link("Edit", to: Routes.rotation_path(@conn, :edit, @changeset.data.section_id, @changeset.data.id)) %></li>
    </ul>
</nav>
<h1 class="title">Edit Rotation:
    <%= @changeset.data.number %>
</h1>
<hr>
<%= form(@conn, @changeset) %>
<script src='<%= Routes.static_path(@conn, "/js/form.js") %>'></script>