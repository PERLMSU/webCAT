<nav class="breadcrumb" aria-label="breadcrumbs">
    <ul>
        <li><%= link("Classrooms", to: Routes.classroom_path(@conn, :index)) %></li>
        <li><%= link(@changeset.data.observation.category.classroom.name, to: Routes.classroom_path(@conn, :show, @changeset.data.observation.category.classroom_id)) %></li>
        <li><%= link("Categories", to: Routes.category_path(@conn, :index, @changeset.data.observation.category.classroom_id)) %></li>
        <li><%= link(@changeset.data.observation.category.name, to: Routes.category_path(@conn, :show, @changeset.data.observation.category.classroom_id, @changeset.data.observation.category_id)) %></li>
        <li><%= link("Observations", to: Routes.observation_path(@conn, :index, @changeset.data.observation.category_id))%></li>
        <li><%= link(truncate(@changeset.data.observation.content, 10), to: Routes.observation_path(@conn, :show, @changeset.data.observation.category_id, @changeset.data.observation_id)) %></li>
        <li><%= link("Feedback", to: Routes.feedback_path(@conn, :index, @changeset.data.observation_id)) %></li>
        <li><%= link(truncate(@changeset.data.content, 10), to: Routes.feedback_path(@conn, :show, @changeset.data.observation_id, @changeset.data.id)) %></li>
        <li class="is-active"><%= link("Edit", to: Routes.feedback_path(@conn, :edit, @changeset.data.observation_id, @changeset.data.id)) %></li>
    </ul>
</nav>
<h1 class="title">Edit Feedback
</h1>
<hr>
<%= form(@conn, @changeset) %>
<script src='<%= Routes.static_path(@conn, "/js/form.js") %>'></script>