<div class="columns">
    <%= if not Enum.empty?(@groups) do %>
    <%= for group <- @groups do %>
    <div class="column is-one-third">
        <div class="card">
            <header class="card-header">
                <p class="card-header-title">
                    <%= link("Group #{group.number}", to: Routes.feedback_path(@conn, :students, group.id)) %>
                </p>
            </header>
            <div class="card-content">
                <ul>
                    <%= for student <- group.students do %>
                    <li>
                        <%= if student.user != nil do %>
                        <%= student.user.first_name %> <%= student.user.last_name %>
                        <% else %>
                        <%= student.email %>
                        <% end %>
                    </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </div>
    <% end %>
    <% else %>
    <h1>No groups</h1>
    <% end %>
</div>