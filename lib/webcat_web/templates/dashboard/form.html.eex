<section class="section">
    <div class="container">
        <h1 class="title"><%= if @changeset.data.id, do: "Edit", else: "Create New" %> <%= String.capitalize(item_name()) %></h1>
        <%= generate_form(@changeset)%>
    </div>
</section>