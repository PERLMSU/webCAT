<div class="ui grid container">
    <!-- Navigation Breadcrumbs -->
    <div class="row">
        <div class="eight wide column">
                <%= breadcrumbs([
                    breadcrumb("Classrooms", to: Routes.classroom_path(@conn, :index)),
                    breadcrumb(@classroom.name, to: Routes.classroom_path(@conn, :show, @classroom.id)),
                    breadcrumb("Categories", to: Routes.category_path(@conn, :index, classroom_id: @classroom.id)),
                    breadcrumb("New")
                ]) %>
        </div>
    </div>
    <div class="row">
        <div class="column">
            <h1 class="header">New Category</h1>
        </div>
    </div>
    <div class="row">
        <div class="column">
            <div class="ui raised segment">
                <%= render("form.html", assigns) %>
            </div>
        </div>
    </div>
</div>