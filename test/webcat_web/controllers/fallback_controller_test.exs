defmodule WebCATWeb.FallbackControllerTest do
  use WebCATWeb.ConnCase

  describe "call/2" do
    test "handles 404 errors", %{conn: conn} do
      flunk("Test needs to be written")
    end

    test "handles authorization errors", %{conn: conn} do
      flunk("Test needs to be written")
    end

    test "handles other errors", %{conn: conn} do
      flunk("Test needs to be written")
    end
  end
end
