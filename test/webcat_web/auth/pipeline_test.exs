defmodule WebCATWeb.Auth.PipelineTest do
  use WebCATWeb.ConnCase

  test "verifies the session", %{conn: conn} do
    flunk("Test needs to be written")
  end

  test "verifies the header", %{conn: conn} do
    flunk("Test needs to be written")
  end
end
