defmodule WebCATWeb.Auth.GuardianTest do
  use WebCATWeb.ConnCase

  describe "subject_for_token/2" do
    test "it handles a user", %{conn: conn} do
      flunk("Test needs to be written")
    end

    test "it handles an unknown resource type", %{conn: conn} do
      flunk("Test needs to be written")
    end
  end

  describe "resource_from_claims/1" do
    test "it handles a user", %{conn: conn} do
      flunk("Test needs to be written")
    end

    test "it handles an unknown resource type", %{conn: conn} do
      flunk("Test needs to be written")
    end
  end
end
