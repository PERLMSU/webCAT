defmodule WebCAT.CRUDTest do
  use WebCAT.DataCase, async: true

  alias WebCAT.CRUD

  describe("list/2") do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end
  end

  describe("get/2") do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end

    test "errors when id not found" do
      flunk("Test needs to be written")
    end
  end

  describe("create/2") do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end
  end

  describe("update/2") do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end

    test "errors when id not found" do
      flunk("Test needs to be written")
    end
  end

  describe("delete/2") do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end

    test "errors when id not found" do
      flunk("Test needs to be written")
    end
  end
end
