defmodule WebCAT.ValidatorsTest do
  use WebCAT.DataCase, async: true

  alias WebCAT.Validators
  import Ecto.Changeset

  describe("validate_dates_after/4") do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end
  end
end
