defmodule WebCAT.Accounts.TokenCredentialTest do
  use WebCAT.DataCase, async: true

  alias WebCAT.Accounts.TokenCredential

  describe "changeset/2" do
    test "behaves as expected" do
      flunk("Test needs to be written")
    end
  end
end
